export const gameTimeCountdownAnnouncement = (hours, minutes, seconds) => {
    const timeInSeconds = (hours * 60 * 60) + (minutes * 60) + seconds;

    if (timeInSeconds === 120 + 1) {
        console.log(`Play file named ${timeInSeconds}.mp3`);
        return true;
    }

    if (timeInSeconds === 90 + 1) {
        console.log(`Play file named ${timeInSeconds}.mp3`);
        return true;
    }

    if (timeInSeconds === 60 + 1) {
        console.log(`Play file named ${timeInSeconds}.mp3`);
        return true;
    }

    if (timeInSeconds === 30 + 1) {
        console.log(`Play file named ${timeInSeconds}.mp3`);
        return true;
    }

    if (timeInSeconds === 20 + 1) {
        console.log(`Play file named ${timeInSeconds}.mp3`);
        return true;
    }

    if (timeInSeconds === 15 + 1) {
        console.log(`Play file named ${timeInSeconds}.mp3`);
        return true;
    }

    if (timeInSeconds === 10 + 1) {
        console.log(`Play file named ${timeInSeconds}.mp3`);
        return true;
    }

    if (timeInSeconds === 9 + 1) {
        console.log(`Play file named ${timeInSeconds}.mp3`);
        return true;
    }

    if (timeInSeconds === 8 + 1) {
        console.log(`Play file named ${timeInSeconds}.mp3`);
        return true;
    }

    if (timeInSeconds === 7 + 1) {
        console.log(`Play file named ${timeInSeconds}.mp3`);
        return true;
    }

    if (timeInSeconds === 6 + 1) {
        console.log(`Play file named ${timeInSeconds}.mp3`);
        return true;
    }

    if (timeInSeconds === 5 + 1) {
        console.log(`Play file named ${timeInSeconds}.mp3`);
        return true;
    }

    if (timeInSeconds === 4 + 1) {
        console.log(`Play file named ${timeInSeconds}.mp3`);
        return true;
    }

    if (timeInSeconds === 3 + 1) {
        console.log(`Play file named ${timeInSeconds}.mp3`);
        return true;
    }

    if (timeInSeconds === 2 + 1) {
        console.log(`Play file named ${timeInSeconds}.mp3`);
        return true;
    }

    if (timeInSeconds === 1 + 1) {
        console.log(`Play file named ${timeInSeconds}.mp3`);
        return true;
    }

    if (timeInSeconds === 0 + 1) {
        console.log(`Play file named ${timeInSeconds}.mp3`);
        return true;
    }

    return false;
}
