export const gameTimeCountdownAnnouncement = (hours, minutes, seconds) => {
    const timeInSeconds = (hours * 60 * 60) + (minutes * 60) + seconds;

    if (timeInSeconds === 120) {
        console.log(`Play file named ${timeInSeconds}.mp4`);
    }

    if (timeInSeconds === 90) {
        console.log(`Play file named ${timeInSeconds}.mp4`);
    }

    if (timeInSeconds === 60) {
        console.log(`Play file named ${timeInSeconds}.mp4`);
    }

    if (timeInSeconds === 30) {
        console.log(`Play file named ${timeInSeconds}.mp4`);
    }

    if (timeInSeconds === 20) {
        console.log(`Play file named ${timeInSeconds}.mp4`);
    }

    if (timeInSeconds === 15) {
        console.log(`Play file named ${timeInSeconds}.mp4`);
    }

    if (timeInSeconds === 10) {
        console.log(`Play file named ${timeInSeconds}.mp4`);
    }

    if (timeInSeconds === 9) {
        console.log(`Play file named ${timeInSeconds}.mp4`);
    }

    if (timeInSeconds === 8) {
        console.log(`Play file named ${timeInSeconds}.mp4`);
    }

    if (timeInSeconds === 7) {
        console.log(`Play file named ${timeInSeconds}.mp4`);
    }

    if (timeInSeconds === 6) {
        console.log(`Play file named ${timeInSeconds}.mp4`);
    }

    if (timeInSeconds === 5) {
        console.log(`Play file named ${timeInSeconds}.mp4`);
    }

    if (timeInSeconds === 4) {
        console.log(`Play file named ${timeInSeconds}.mp4`);
    }

    if (timeInSeconds === 3) {
        console.log(`Play file named ${timeInSeconds}.mp4`);
    }

    if (timeInSeconds === 2) {
        console.log(`Play file named ${timeInSeconds}.mp4`);
    }

    if (timeInSeconds === 1) {
        console.log(`Play file named ${timeInSeconds}.mp4`);
    }
}